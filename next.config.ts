import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Tell Next.js that the app/ directory is inside src/frontend/
  // This resolves the "Couldn't find any pages or app directory" error
};

export default nextConfig;
